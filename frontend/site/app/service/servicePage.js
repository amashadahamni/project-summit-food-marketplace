const API = "http://localhost:8080/api/v1"; const status = document.getElementById("accountStatus"); const form = document.getElementById("profileForm"); const displayName = document.getElementById("displayName");
async function loadProfile() { const response = await fetch(`${API}/users/me`, { credentials:"include" }); const profile = await response.json(); if (!response.ok) throw new Error(response.status === 401 ? "Log in to view and edit your account." : profile.error || "Account unavailable."); status.textContent = `Signed in with roles: ${profile.roles.join(", ") || "Customer"}`; document.getElementById("email").textContent = profile.email || "No email returned by the identity provider."; displayName.value = profile.display_name || ""; form.hidden = false; }
form.addEventListener("submit", async (event) => { event.preventDefault(); const response = await fetch(`${API}/users/me`, { method:"PATCH", credentials:"include", headers:{"Content-Type":"application/json"}, body:JSON.stringify({ display_name:displayName.value }) }); status.textContent = response.ok ? "Profile saved." : "Unable to save profile."; }); loadProfile().catch((error) => status.textContent = error.message);