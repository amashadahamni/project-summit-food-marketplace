const API = "http://localhost:8080/api/v1"; const list = document.getElementById("cartList"); const status = document.getElementById("cartStatus");
async function loadCart() { const response = await fetch(`${API}/cart`, { credentials:"include" }); const cart = await response.json(); if (!response.ok) throw new Error(response.status === 401 ? "Log in to view your cart." : cart.error || "Cart unavailable."); status.textContent = cart.items.length ? "" : "Your cart is empty."; list.innerHTML = cart.items.map((item) => `<article class="cart-item"><div><h2>Product #${item.product_id}</h2><p>Quantity: ${item.quantity}</p></div><a class="button" href="product.html?id=${item.product_id}">View</a><button class="remove" data-product-id="${item.product_id}" type="button">Remove</button></article>`).join(""); document.querySelectorAll(".remove").forEach((button) => button.addEventListener("click", () => removeItem(button.dataset.productId))); }
async function removeItem(productId) { const response = await fetch(`${API}/cart/items/${productId}`, { method:"DELETE", credentials:"include" }); if (!response.ok) { status.textContent = "Unable to remove that item."; return; } loadCart().catch((error) => status.textContent = error.message); }
loadCart().catch((error) => status.textContent = error.message);